// top-level facade for all Sudoku
// also defines operations

class Sudoku {
  // create( ... ): string // sudokustring // parameters could define complexity? unsolved cells and possible solutions possibly impossible combination.. // maybe get sudokus from a db? // or obtain from tree and count 'splits'
  // solve( sudoku ): Array<string> // a series of sudokustrings that are valid solutions
}
